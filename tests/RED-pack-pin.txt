The first run of tests\run-pack-pin-tests.ps1, before check-pack-pin.ps1 existed and before
publish-pack.ps1 grew its -GhPath seam, its existing-tag refusal, or -Promote. Twelve arms,
twelve failures, each on its own assertion.

PIN-01..PIN-06 fail because tools\check-pack-pin.ps1 is not there; PIN-06 in particular is the
arm most able to pass while proving nothing, since "refuses" is also true of a guard that does
not exist, so it additionally requires the refusal to name the absent resource-pack line.

IMM-01, PROM-01 and PROM-04 assert a refusal plus an empty upload log, both of which a script
that never runs satisfies. Each therefore also requires evidence the script reached its
decision: that gh was consulted about the tag, and that the printed refusal names the tag or
the two sha1s it is about. Without those controls this suite reported 3 passes against no
implementation at all.

  PIN - the prod pre-start guard
    FAIL PIN-01  a prod config pinned under the dev release is refused
    FAIL PIN-02  a prod config whose asset name ends -dev.zip is refused
    FAIL PIN-03  a prod config whose pinned sha1 does not match the asset is refused, naming both
    FAIL PIN-04  a prod config pinned to a matching versioned asset passes
    FAIL PIN-05  mc-dev is exempt by name, and says so
    FAIL PIN-06  a config with no resource-pack line is refused, naming the absent pin

  IMM - a versioned release is immutable
    FAIL IMM-01  publishing over an existing v<version> tag is refused, and uploads nothing
    FAIL IMM-02  the rolling dev pre-release is still clobbered

  PROM - promotion is the only dev-to-versioned path
    FAIL PROM-01  promotion refuses on a sha1 mismatch, naming both sha1s, and uploads nothing
    FAIL PROM-02  promotion uploads the dev bytes unchanged to v<version>
    FAIL PROM-03  promotion prints the prod server.properties lines
    FAIL PROM-04  promotion refuses when the target version already exists

  0 passed, 12 failed
